import React from "react";
import { Section } from "./section";
import "./header.css";

export const Header = ({}) => (
  <Section>
    <header className="header">
      <h1>Cs:Go Buy Binds</h1>
    </header>
  </Section>
);
