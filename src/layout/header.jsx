import React from 'react';
import { Section } from './section';
import './header.scss';

export const Header = ({}) => (
  <Section>
    <header className="header">Cs:Go Buy Binds</header>
  </Section>
);
